// Here we need to handle everything about shift (filter, take)

import { ShiftElement } from "../interfaces/interface";

export const filterShifts = async (
  shifts: ShiftElement[]
): Promise<ShiftElement[]> => {
  return shifts;
};
